/**
 * Task: Parse a CSV string into a 2D array.
 * Follow these steps sequentially to build the function:
 */

function parseCSV(csvString) {
    // Step 1: Handle different newline characters (\n, \r\n) to split rows.
    
    // Step 2: Iterate through rows and split by comma delimiter.
    
    // Step 3: Handle edge case where commas are enclosed within double quotes (e.g., "New York, NY").
    
    // Step 4: Remove the utility quotes from the final values.
    
    // Step 5: Return the final 2D array.
}
