/**
 * Task: Check if a string is a palindrome.
 * 
 * Examples:
 * - isPalindrome("racecar") -> true
 * - isPalindrome("hello") -> false
 * - isPalindrome("A man, a plan, a canal: Panama") -> true (should ignore casing and punctuation)
 */

function isPalindrome(str) {
    // TODO: Implement logic based on the examples above
}
