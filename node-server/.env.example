# Environment Variables Configuration
# Copy this file to .env and customize the values for your environment

# =============================================================================
# Server Configuration
# =============================================================================

# Server Port
# The port number on which the server will listen for incoming requests
# Default: 8000
# Valid values: 1024-65535 (avoid ports below 1024 which require root privileges)
PORT=8000

# Node Environment
# Determines the application's behavior and logging level
# Default: development
# Valid values: development, production, test
NODE_ENV=development

# =============================================================================
# CORS Configuration
# =============================================================================

# CORS Allowed Origins
# Specifies which domains can access the API
# Use '*' for development (allows all origins)
# Use specific domain(s) in production for security
# Default: *
# Examples:
#   - Single domain: https://yourdomain.com
#   - Multiple domains (comma-separated): https://domain1.com,https://domain2.com
#   - Local development: http://localhost:3000
CORS_ORIGIN=*

# =============================================================================
# Usage Examples
# =============================================================================

# Development Configuration:
# PORT=8000
# NODE_ENV=development
# CORS_ORIGIN=*

# Production Configuration:
# PORT=8000
# NODE_ENV=production
# CORS_ORIGIN=https://yourdomain.com

# Testing Configuration:
# PORT=8001
# NODE_ENV=test
# CORS_ORIGIN=*

# =============================================================================
# Notes
# =============================================================================
#
# 1. Never commit the .env file to version control
#    (it's already in .gitignore)
#
# 2. Each developer should have their own .env file with values
#    appropriate for their local environment
#
# 3. Production environments should use environment variables
#    set directly in the hosting platform (Docker, AWS, Heroku, etc.)
#
# 4. For Docker deployments, environment variables can be set in:
#    - docker-compose.yml (environment: section)
#    - .env file (read by Docker Compose)
#    - docker run --env flags
#
# 5. See ENVIRONMENT.md for detailed documentation on all variables
#
